version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:18.3.0
    steps:
      - checkout
      - run: node --version
      - run:
          name: The First Step
          command: echo 'Hello World!'
      - run:
          name: Install Dependencies
          command: yarn install
      - run:
          name: Build Next app
          command: |
            yarn build
